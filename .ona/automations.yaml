tasks:
  install-aws-cli:
    name: Install AWS CLI
    description: Install AWS CLI v2 and enable auto-prompt
    command: |
      curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o /tmp/awscliv2.zip
      unzip -q /tmp/awscliv2.zip -d /tmp
      sudo /tmp/aws/install --update
      rm -rf /tmp/awscliv2.zip /tmp/aws
      echo "AWS CLI $(aws --version) installed"
    triggeredBy:
      - postDevcontainerStart

  install-languages:
    name: Install Java and Ruby
    command: |
      sudo apt-get update
      sudo apt-get install -y openjdk-21-jdk ruby-full

      echo "Java: $(java --version)"
      echo "Ruby: $(ruby -v)"
    triggeredBy:
      - postDevcontainerStart

  install-terraform:
    name: Install Terraform
    command: |
        sudo apt-get update && sudo apt-get install -y gnupg software-properties-common
        wget -O- https://apt.releases.hashicorp.com/gpg | \
          gpg --dearmor | \
          sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg > /dev/null
        gpg --no-default-keyring \
          --keyring /usr/share/keyrings/hashicorp-archive-keyring.gpg \
          --fingerprint
        echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(grep -oP '(?<=UBUNTU_CODENAME=).*' /etc/os-release || lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
        sudo apt-get update
        sudo apt-get install -y terraform
    triggeredBy:
      - postDevcontainerStart
  
  install-node: 
    name: Install Node and CDK
    command: |
      curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
      sudo apt-get install -y nodejs
      sudo  npm i -g aws-cdk
      echo "Node: $(node --version)"
      echo "NPM: $(npm --version)"
      echo "CDK: $(cdk --version)"


  #install-powershell:
    #name: Install PowerShell
    #description: Install PowerShell via Microsoft repository
    #command: |
      #sudo apt-get update
      #sudo apt-get install -y wget apt-transport-https software-properties-common
      #source /etc/os-release
      #wget -q https://packages.microsoft.com/config/ubuntu/$VERSION_ID/packages-microsoft-prod.deb
      #sudo dpkg -i packages-microsoft-prod.deb
      #rm packages-microsoft-prod.deb
      #sudo apt-get update
      #sudo apt-get install -y powershell
      #echo "PowerShell $(pwsh --version) installed"
    #triggeredBy:
    #  - postDevcontainerStart
    #Install-Module -Name AWS.Tools.Installer